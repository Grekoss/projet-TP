{% extends 'base.html.twig' %}

{% block title %}Signalement d'un évènement{% endblock %}

{% block body %}
    <h1 class="h1">Signalement d'un évènement</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-success">{{ button_label|default('Valider') }}</button>
    {{ form_end(form) }}

{% endblock %}