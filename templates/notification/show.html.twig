{% extends 'base.html.twig' %}

{% block title %}Notification{% endblock %}

{% block body %}
    <h1>Notification</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Titre</th>
                <td>{{ notification.title }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{notification.body|raw}}</td> 
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_notifications', {'id': notification.sendee.id}) }}">Retour à la liste</a>
    
    {{ include('notification/_delete_form.html.twig') }}
{% endblock %}