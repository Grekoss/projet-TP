<form method="post" action="{{ path('comment_delete', {'id': comment.id}) }}" onsubmit="return confirm('Etes vous sûre de supprimer votre commentaire ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comment.id) }}">
    <button title= "Supprimer le commentaire" class="btn"><i class="fas fa-trash-alt"></i></button>
</form>